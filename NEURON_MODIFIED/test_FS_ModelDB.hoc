nrnpython("from run_cell import run_cell")

nrnpython("run_cell('FS', \
                    nogui      = True, \
                    duration   = 1000, \
                    dt         = 0.001, \
                    idelay     = 300, \
                    iduration  = 400, \
                    iamp       = 0.5, \
                    v_init     = -70)")

